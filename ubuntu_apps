base = {
  'packages' : [
    git-core,
  ]
}

server = {
  'packages' : [
    tmux,
  ]
}

desktop = {
  'ppa': [flozz/flozz],
  'packages' : [
    ubuntu-restricted-extras,
    python-pip,
    chromium,
    gnome-do,
    parcellite,
    nautilus-open-terminal,
    nautilus-terminal,
  ]
}
